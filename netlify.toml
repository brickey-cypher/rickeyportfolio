[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  port = 4323
  # targetPort = 4321
  publish = "dist"
  functions = "netlify/functions"
